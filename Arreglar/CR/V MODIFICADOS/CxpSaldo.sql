SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER VIEW CxpSaldo

AS
SELECT
s.Empresa,
s.Moneda,
s.Cuenta Proveedor,
Convert(money, Sum(s.Saldo*r.Factor)) Saldo
FROM Saldo s WITH (NOLOCK)
JOIN Rama r WITH (NOLOCK) ON s.Rama=r.Rama AND r.Mayor='CXP'
GROUP BY
s.Empresa, s.Moneda, s.Cuenta

