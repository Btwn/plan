SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER VIEW eCommerceArt

AS
SELECT Articulo, Rama, Descripcion1, Descripcion2, NombreCorto, Grupo, Categoria, Familia, Linea, Fabricante, ClaveFabricante, Estatus, Tipo
FROM Art WITH (NOLOCK)
WHERE Articulo NOT IN (SELECT Articulo FROM WebArt WITH (NOLOCK))

