SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER VIEW pNetRepLogRecibosNomina
AS
SELECT l.*, dbo.fnFechaSinHora(FechaRegistro) FechaRegistroSinHora, LTRIM(RTRIM(p.NombreCompleto)) Nombre
FROM LogRecibosNomina l WITH(NOLOCK) JOIN PersonalA p WITH(NOLOCK) ON l.Usuario = p.Personal

