SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER FUNCTION fnClasificaABC (@Valor float)
RETURNS char
AS
BEGIN
DECLARE @ABC char(1)
SELECT @ABC = ABC
FROM ABC
WHERE @Valor BETWEEN Desde AND Hasta
RETURN @ABC
END

