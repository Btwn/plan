SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER FUNCTION fnWebPaginaParamValor (@SesionID uniqueidentifier, @Pagina varchar(20), @Parametro varchar(50))
RETURNS varchar(7000)

AS BEGIN
RETURN(SELECT Valor FROM WebSesionPaginaParam WHERE SesionID=@SesionID AND Pagina=@Pagina AND Parametro=@Parametro)
END

