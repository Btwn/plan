SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER PROCEDURE spPOSPuedePesar
@ID			varchar(36)

AS
BEGIN
DECLARE
@Respuesta	bit
SET @Respuesta = 0
IF EXISTS (SELECT * FROM POSLArtSeleccionado WITH (NOLOCK) WHERE ID = @ID)
SELECT @Respuesta = 1
ELSE
SELECT @Respuesta = 0
SELECT @Respuesta
END

