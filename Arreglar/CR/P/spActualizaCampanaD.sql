SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER PROCEDURE spActualizaCampanaD @ID int, @RID Int

AS BEGIN
DECLARE
@FechaD datetime,
@FechaA datetime,
@ListaPreciosEsp varchar(20)
IF @RID IS NOT NULL
BEGIN
SELECT @ListaPreciosEsp = ListaPreciosEsp
FROM CampanaDTemp
WHERE RID = @RID
UPDATE CampanaD
SET ListaPreciosEsp = @ListaPreciosEsp
WHERE ID = @ID AND RID = @RID
END
END

