SET DATEFIRST 7
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER PROCEDURE xpPOSAfectar
@Empresa	varchar(5),
@Modulo		varchar(5),
@Sucursal	int,
@Usuario	varchar(10),
@ID			varchar(36),
@Host 		varchar(20)	= NULL,
@Ok			int				OUTPUT,
@OkRef		varchar(255)    OUTPUT
AS
BEGIN
RETURN
END

