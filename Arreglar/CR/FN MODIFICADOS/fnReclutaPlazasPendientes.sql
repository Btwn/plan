SET DATEFIRST 7    
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1  
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER FUNCTION fnReclutaPlazasPendientes (@ID int)
RETURNS int

AS BEGIN
DECLARE
@Resultado	int
SELECT @Resultado = COUNT(*) FROM ReclutaPlaza WITH(NOLOCK) WHERE ID = @ID AND EstaPendiente = 1
RETURN(NULLIF(@Resultado, 0))
END

