SET DATEFIRST 7    
SET ANSI_NULLS OFF
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1  
SET QUOTED_IDENTIFIER OFF
SET NOCOUNT ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER FUNCTION fnTipoImpuestoTasa (@TipoImpuesto varchar(10))
RETURNS float

AS BEGIN
RETURN(SELECT Tasa FROM TipoImpuesto WITH(NOLOCK) WHERE TipoImpuesto=@TipoImpuesto)
END

