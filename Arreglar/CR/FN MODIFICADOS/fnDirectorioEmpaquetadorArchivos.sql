SET DATEFIRST 7    
SET ANSI_NULLS ON
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
SET LOCK_TIMEOUT -1  
SET QUOTED_IDENTIFIER ON
SET NOCOUNT ON
SET ANSI_WARNINGS ON
SET IMPLICIT_TRANSACTIONS OFF
GO
ALTER FUNCTION fnDirectorioEmpaquetadorArchivos()
RETURNS varchar(255)
AS
BEGIN
DECLARE @Directorio		varchar(255)
SELECT @Directorio = dbo.fnDirectorioEliminarDiagonalFinal(DirectorioEmpaquetadorArchivos) FROM Version WITH(NOLOCK)
RETURN @Directorio
END

